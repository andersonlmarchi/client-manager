package shared

// ModuleName is the canonical module path fragment for this package.
const ModuleName = "packages/shared"

// Version is the bootstrap package version (semver placeholder until release tags).
const Version = "0.0.0"
